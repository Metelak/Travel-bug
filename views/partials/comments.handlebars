<div class="comments">
	{{#each this}}
		<section class="comment my-4">
			<div class="meta">
				{{user.username}}
				on
				{{format_date createdAt}}
			</div>
			<div class="text">
				{{comment_text}}
			</div>
		</section>
	{{/each}}
</div>